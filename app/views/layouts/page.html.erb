<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= @title %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag 'client', recursive: false, media: 'all', 'data-turbolinks-track': 'reload' %>

  </head>

  <body>
    <header class="container-fluid head">
      <div class="row">
        <div class="col-md-8 col-md-offset-2 my-nav">
          <!-- Brand and toggle for mobile display -->
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-main" aria-expanded="false">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <h3 class="text-muted"><%= link_to 'Drawler', root_path %></h3>
          </div>
          <!-- Collect the nav links, forms, and other content for toggling -->
          <nav class="collapse navbar-collapse" id="navbar-main">
            <ul class="nav nav-pills pull-right">
              <li role="presentation"><%= link_to 'Trang Chủ', root_path %></li>
              <%# load categories %>
              <% if @categories.any?  %>
                  <% @categories.each do |category|  %>
                      <li role="presentation"><%= link_to category.name, category, class: 'text-capitalize' %></li>
                  <% end  %>
              <% end  %>
            </ul>
          </nav><!-- /.navbar-collapse -->
        </div>
      </div>
    </header> <!--/header-->

    <div class="container-fluid main">
      <div class="row">
        <div class="col-md-8 col-md-offset-2">
          <%= yield %>
        </div>
      </div>
    </div> <!-- /main-->

    <footer class="container-fluid">
      <div class="row">
        <div class="col-md-8 col-md-offset-2 text-center">
          <p>&copy; haoguyen4993@gmail.com</p>
        </div>
      </div>
    </footer> <!-- /footer -->
    <%= javascript_include_tag 'client', recursive: false, 'data-turbolinks-track': 'reload' %>
  </body>
</html>